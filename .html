<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>مسابقة كأس العالم 2026 — Outfit × همكس</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;600;700;900&family=Bebas+Neue&display=swap" rel="stylesheet">

  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background: #0d0d0d;
      font-family: 'Cairo', sans-serif;
      direction: rtl;
      min-height: 100vh;
    }

    .top-banner {
      background: #006C35;
      padding: 10px 28px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 14px;
      flex-wrap: wrap;
      text-align: center;
    }

    .banner-text {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 13px;
      color: #fff;
      letter-spacing: 4px;
    }

    .banner-sub {
      font-size: 11px;
      color: rgba(255,255,255,0.7);
      font-weight: 600;
    }

    .header {
      background: #0d0d0d;
      padding: 20px 28px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-bottom: 1px solid #1a1a1a;
      gap: 16px;
    }

    .logo-col { display: flex; flex-direction: column; }

    .logo-outfit {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 42px;
      color: #fff;
      letter-spacing: 6px;
      line-height: 1;
    }

    .logo-x {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 11px;
      color: #FF5C00;
      letter-spacing: 3px;
      margin-top: 2px;
    }

    .right-col {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: 6px;
    }

    .presenter {
      font-size: 11px;
      color: #444;
      font-weight: 600;
    }

    .hmx-tag {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 22px;
      color: #FF5C00;
      letter-spacing: 3px;
      line-height: 1;
    }

    .hero {
      background: #0d0d0d;
      padding: 0 28px;
      position: relative;
      overflow: hidden;
    }

    .hero-stripe {
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 4px;
      background: linear-gradient(90deg, #FF5C00 0%, #FF5C00 50%, #006C35 50%, #006C35 100%);
    }

    .hero-inner {
      padding: 30px 0 26px;
      text-align: center;
      position: relative;
    }

    .hero-eyebrow {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 11px;
      color: #006C35;
      letter-spacing: 5px;
      margin-bottom: 8px;
    }

    .hero-ar {
      font-size: 28px;
      font-weight: 900;
      color: #fff;
      line-height: 1.2;
    }

    .hero-ar span { color: #FF5C00; }

    .hero-en {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 13px;
      color: #444;
      letter-spacing: 4px;
      margin-top: 6px;
    }

    .hero-divider {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      margin-top: 16px;
    }

    .hd-line { flex: 1; max-width: 60px; height: 1px; }
    .hd-left { background: #FF5C00; }
    .hd-right { background: #006C35; }
    .hd-dot { width: 6px; height: 6px; background: #fff; border-radius: 50%; }

    .form-body {
      padding: 28px 28px 36px;
      max-width: 560px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: 0;
    }

    .field {
      display: flex;
      flex-direction: column;
      gap: 7px;
      padding: 20px 0;
      border-bottom: 1px solid #1a1a1a;
    }

    .field:last-of-type { border-bottom: none; }

    .field-num {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 11px;
      color: #FF5C00;
      letter-spacing: 2px;
      margin-bottom: 2px;
    }

    .lbl-ar {
      font-size: 16px;
      font-weight: 700;
      color: #fff;
      line-height: 1;
    }

    .lbl-en {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 10px;
      color: #555;
      letter-spacing: 3px;
      margin-top: 2px;
    }

    .finput {
      background: #141414;
      border: 1.5px solid #222;
      border-radius: 6px;
      padding: 13px 15px;
      color: #fff;
      font-family: 'Cairo', sans-serif;
      font-size: 14px;
      direction: rtl;
      outline: none;
      transition: border-color 0.2s, background 0.2s;
      width: 100%;
      margin-top: 6px;
    }

    .finput:focus {
      border-color: #FF5C00;
      background: #180e00;
    }

    .finput::placeholder {
      color: #333;
      font-size: 13px;
    }

    .error-text {
      color: #ff7b7b;
      font-size: 12px;
      font-weight: 600;
      display: none;
      margin-top: 4px;
    }

    .btn-wrap { padding-top: 24px; }

    .btn-submit {
      width: 100%;
      border: none;
      border-radius: 6px;
      padding: 0;
      cursor: pointer;
      overflow: hidden;
      display: flex;
      transition: transform 0.1s, opacity 0.2s;
    }

    .btn-submit:active { transform: scale(0.98); }
    .btn-submit:disabled {
      opacity: 0.7;
      cursor: not-allowed;
    }

    .btn-left {
      background: #FF5C00;
      flex: 1;
      padding: 16px;
      font-family: 'Bebas Neue', sans-serif;
      font-size: 18px;
      letter-spacing: 3px;
      color: #fff;
      text-align: center;
    }

    .btn-right {
      background: #006C35;
      flex: 1;
      padding: 16px;
      font-size: 15px;
      font-weight: 700;
      color: #fff;
      text-align: center;
    }

    .status-box {
      display: none;
      margin-top: 18px;
      padding: 12px 14px;
      border-radius: 6px;
      font-size: 14px;
      font-weight: 700;
      text-align: center;
    }

    .status-error {
      background: #2b1111;
      color: #ff9f9f;
      border: 1px solid #5a1f1f;
    }

    .success {
      display: none;
      margin: 0 28px 28px;
      border-radius: 8px;
      overflow: hidden;
    }

    .success-top {
      background: #006C35;
      padding: 20px;
      text-align: center;
      color: #fff;
      font-size: 20px;
      font-weight: 700;
    }

    .success-bottom {
      background: #FF5C00;
      padding: 10px;
      text-align: center;
      font-family: 'Bebas Neue', sans-serif;
      font-size: 13px;
      letter-spacing: 3px;
      color: #fff;
    }

    .footer {
      text-align: center;
      padding: 16px;
      color: #222;
      font-family: 'Bebas Neue', sans-serif;
      font-size: 11px;
      letter-spacing: 3px;
      border-top: 1px solid #161616;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 12px;
      flex-wrap: wrap;
    }

    .f-dot {
      width: 4px;
      height: 4px;
      border-radius: 50%;
    }

    .f-orange { background: #FF5C00; }
    .f-green { background: #006C35; }

    @media (max-width: 640px) {
      .header {
        padding: 18px 18px;
      }

      .hero,
      .form-body {
        padding-left: 18px;
        padding-right: 18px;
      }

      .logo-outfit {
        font-size: 34px;
      }

      .hero-ar {
        font-size: 23px;
      }

      .btn-left,
      .btn-right {
        padding: 14px;
        font-size: 14px;
      }
    }
  </style>
</head>
<body>

  <div class="top-banner">
    <span class="banner-sub">المنتخب السعودي</span>
    <span class="banner-text">🇸🇦 KSA — WORLD CUP 2026</span>
    <span class="banner-sub">الصقور الخضر</span>
  </div>

  <div class="header">
    <div class="logo-col">
      <span class="logo-outfit">OUTFIT</span>
      <span class="logo-x">BRAND × COMPETITION</span>
    </div>
    <div class="right-col">
      <span class="presenter">Presented by</span>
      <span class="hmx-tag">همكس</span>
    </div>
  </div>

  <div class="hero">
    <div class="hero-stripe"></div>
    <div class="hero-inner">
      <div class="hero-eyebrow">SAUDI ARABIA × FIFA 2026</div>
      <div class="hero-ar">مسابقة <span>كأس العالم</span> 2026</div>
      <div class="hero-en">FIFA World Cup 2026 — Registration</div>
      <div class="hero-divider">
        <div class="hd-line hd-left"></div>
        <div class="hd-dot"></div>
        <div class="hd-line hd-right"></div>
      </div>
    </div>
  </div>

  <div class="form-body" id="theForm">

    <div class="field">
      <div class="field-num">01</div>
      <label class="lbl-ar" for="f-name">الاسم</label>
      <div class="lbl-en">Full Name</div>
      <input class="finput" id="f-name" type="text" placeholder="اكتب اسمك الكامل" />
      <div class="error-text" id="err-name">يرجى إدخال الاسم الكامل</div>
    </div>

    <div class="field">
      <div class="field-num">02</div>
      <label class="lbl-ar" for="f-nat">الجنسية</label>
      <div class="lbl-en">Nationality</div>
      <input class="finput" id="f-nat" type="text" placeholder="اكتب جنسيتك" />
      <div class="error-text" id="err-nat">يرجى إدخال الجنسية</div>
    </div>

    <div class="field">
      <div class="field-num">03</div>
      <label class="lbl-ar" for="f-city">المدينة</label>
      <div class="lbl-en">City</div>
      <input class="finput" id="f-city" type="text" placeholder="اكتب مدينتك" />
      <div class="error-text" id="err-city">يرجى إدخال المدينة</div>
    </div>

    <div class="field">
      <div class="field-num">04</div>
      <label class="lbl-ar" for="f-num">الرقم</label>
      <div class="lbl-en">Phone Number</div>
      <input class="finput" id="f-num" type="text" inputmode="tel" placeholder="اكتب رقمك" />
      <div class="error-text" id="err-num">يرجى إدخال رقم جوال صحيح</div>
    </div>

    <div class="btn-wrap">
      <button class="btn-submit" id="submitBtn" type="button" onclick="doSubmit()">
        <div class="btn-left" id="btnLeft">REGISTER</div>
        <div class="btn-right" id="btnRight">سجّل الآن</div>
      </button>
    </div>

    <div class="status-box status-error" id="statusBox"></div>

  </div>

  <div class="success" id="successBox">
    <div class="success-top">✓ تم التسجيل بنجاح! 🇸🇦</div>
    <div class="success-bottom">REGISTRATION SUBMITTED — OUTFIT × همكس</div>
  </div>

  <div class="footer">
    <div class="f-dot f-orange"></div>
    <span>OUTFIT © 2026</span>
    <div class="f-dot f-green"></div>
    <span>PRESENTED BY همكس</span>
    <div class="f-dot f-orange"></div>
  </div>

  <script>
    const SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbx9kFSQFDPogsKnBHUmM7tXbkou2xviqIAueIFXOm84Vp5DdjM4sP3NuAI73REsD0ek/exec';

    const fields = {
      name: document.getElementById('f-name'),
      nationality: document.getElementById('f-nat'),
      city: document.getElementById('f-city'),
      number: document.getElementById('f-num')
    };

    const errors = {
      name: document.getElementById('err-name'),
      nationality: document.getElementById('err-nat'),
      city: document.getElementById('err-city'),
      number: document.getElementById('err-num')
    };

    const submitBtn = document.getElementById('submitBtn');
    const btnLeft = document.getElementById('btnLeft');
    const btnRight = document.getElementById('btnRight');
    const statusBox = document.getElementById('statusBox');

    function resetErrors() {
      Object.values(errors).forEach(el => el.style.display = 'none');
      statusBox.style.display = 'none';
      statusBox.textContent = '';
    }

    function showError(fieldKey, message) {
      if (errors[fieldKey]) {
        errors[fieldKey].textContent = message;
        errors[fieldKey].style.display = 'block';
      }
    }

    function validateForm() {
      resetErrors();

      const name = fields.name.value.trim();
      const nationality = fields.nationality.value.trim();
      const city = fields.city.value.trim();
      const number = fields.number.value.trim();

      let isValid = true;

      if (!name || name.length < 3) {
        showError('name', 'يرجى إدخال الاسم الكامل');
        isValid = false;
      }

      if (!nationality || nationality.length < 2) {
        showError('nationality', 'يرجى إدخال الجنسية');
        isValid = false;
      }

      if (!city || city.length < 2) {
        showError('city', 'يرجى إدخال المدينة');
        isValid = false;
      }

      const phoneRegex = /^[0-9+\s\-()]{6,20}$/;
      if (!number || !phoneRegex.test(number)) {
        showError('number', 'يرجى إدخال رقم جوال صحيح');
        isValid = false;
      }

      return isValid;
    }

    function setLoading(isLoading) {
      submitBtn.disabled = isLoading;
      btnLeft.textContent = isLoading ? 'SENDING...' : 'REGISTER';
      btnRight.textContent = isLoading ? 'جاري الإرسال...' : 'سجّل الآن';
    }

    async function doSubmit() {
      if (!validateForm()) return;

      const name = fields.name.value.trim();
      const nationality = fields.nationality.value.trim();
      const city = fields.city.value.trim();
      const number = fields.number.value.trim();

      const formData = new FormData();
      formData.append('name', name);
      formData.append('nationality', nationality);
      formData.append('city', city);
      formData.append('number', number);

      try {
        setLoading(true);

        await fetch(SCRIPT_URL, {
          method: 'POST',
          body: formData,
          mode: 'no-cors'
        });

        document.getElementById('theForm').style.display = 'none';
        document.getElementById('successBox').style.display = 'block';
      } catch (error) {
        console.error(error);
        statusBox.textContent = 'حدث خطأ أثناء الإرسال، يرجى المحاولة مرة أخرى';
        statusBox.style.display = 'block';
      } finally {
        setLoading(false);
      }
    }
  </script>

</body>
</html>
